# ============ Core ============
ENV=dev

# ============ OpenAI ============
OPENAI_API_KEY=replace_me
OPENAI_MODEL=gpt-4o-mini

# ============ Azure OpenAI ============
# Use either (A) endpoint+api_key OR (B) Azure AD token (advanced).
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE-NAME.openai.azure.com/
AZURE_OPENAI_API_KEY=replace_me
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini-deployment

# Optional: if you use AAD auth instead of api key (leave blank otherwise)
AZURE_OPENAI_AD_TOKEN=

# ============ Ollama (Local) ============
# If you run ollama on host or via compose service, set base URL accordingly
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama3.1:8b-instruct

# ============ Other LLM providers (placeholders) ============
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
COHERE_API_KEY=

# ============ Jupyter ============
JUPYTER_TOKEN=devtoken
